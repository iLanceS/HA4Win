<UserControl x:Class="ha4win.Controls.DoubleTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ha4win.Controls"
             mc:Ignorable="d" 
             x:Name="this"
             >
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding ElementName=this,Path=V1}"  VerticalAlignment="Center"  HorizontalAlignment="Right"></TextBlock>
        <TextBox Grid.Column="1" Text="{Binding ElementName=this,Path=V2}" VerticalContentAlignment="Center" VerticalAlignment="Center" BorderThickness="1" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch"></TextBox>
    </Grid>
</UserControl>
